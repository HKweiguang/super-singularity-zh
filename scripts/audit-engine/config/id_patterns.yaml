# 编号格式定义
# 键为前缀，值为正则表达式（不含锚定符，引擎会自动添加 ^$）

patterns:
  REQ: "REQ-\\d{3,4}"
  UI-FLOW: "UI-FLOW-\\d{3,4}"
  UI: "UI-\\d{3,4}"
  API: "API-\\d{3,4}"
  TEST: "TEST-\\d{3,4}"
  DEPLOY: "DEPLOY-\\d{3,4}"
  # 通用示例
  RES: "RES-\\d{3,4}"
  COMP: "COMP-\\d{3,4}"
